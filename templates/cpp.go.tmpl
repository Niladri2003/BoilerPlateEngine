{{.Includes}}
struct ListNode {
    int val;
    ListNode* next;
    ListNode(int x) : val(x), next(nullptr) {}
};
class Solution {
public:
    {{.ReturnType}} {{.FunctionName}}({{.Parameters}}) {
        // Implement here
    }
};
int main() {
    Solution sol;
    // Simplified driver (add test case parsing logic here)
    return 0;
}